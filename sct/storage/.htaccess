# Blokkeer alle directe HTTP-toegang tot de versleutelde bestanden.
# Alleen PHP (fopen/readfile) kan deze map benaderen.
Order allow,deny
Deny from all

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
